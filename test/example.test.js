// test/example.test.js
const { expect } = require('chai');

describe('Simple Math', function() {
  it('should add 1 + 2 to equal 3', function() {
    expect(1 + 2).to.equal(3);
  });
});

describe('Simple Math', function() {
    it('should multiply 2 * 2 to equal 4', function() {
      expect(2 * 2).to.equal(4);
    });
  });

describe('Simple Math', function() {
  it('should divide 18 / 3 to equal 6', function() {
    expect(18 / 3).to.equal(6);
  });
});